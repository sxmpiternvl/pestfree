{
  "name": "monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --parallel --filter='*' dev",
    "build": "pnpm --filter='*' build",
    "lint": "pnpm --filter='*' lint"
  }
}
